pluginManagement {
    repositories {
        google {
            content {
                includeGroupByRegex("com\\.android.*")
                includeGroupByRegex("com\\.google.*")
                includeGroupByRegex("androidx.*")
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "FIND_YOUR_MIND"
include(":app")

include(":app", ":unityLibrary")
project(":unityLibrary").projectDir = File("C:/Unity_Projects/Python_Unity_ahlee/AndroidFiles/unityLibrary")

